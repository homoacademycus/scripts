# envar for docker-compose
COMPOSE_PROJECT_NAME="test"

# HOST
HOST_WORK_DIR=/home/programmer/workspace
HOST_USER=programmer

# CONTAINER
CONTAINER_USER=containeruser
CONTAINER_GROUP=1001
WORKING_DIR=/home/containeruser/workspace

# TO DOCKERFILE
NODEWEB_STATIC_DIR=/nodeApp/static

# FOR DEVELOP
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
MONGO_URI=mongodb://mongodbContainerName:port/myPost

# DB ROOT PASSWORD
MYSQL_PASSWORD=testpswd
COUCHDB_PASSWORD=dbpasswd
POSTGRES_PASSWORD=dbpasswd

# DB NON-ROOT USER
MYSQL_USER=mysql
COUCHDB_USER=dbadmin
POSTGRES_USER=postgres

# DATABASE CREATED
INIT_DATABASE=init
POSTGRES_DATABASE=postgres

IP_ALLOWED=0.0.0.0
#IP_ALLOWED=localhost

# CONTAINER VOLUME - WEB SERVICE
NGINX_CFG_DIR=/etc/nginx
NGINX_LOG_DIR=/var/log/nginx/log
NGINX_STATIC_DIR=/usr/share/nginx/html
JEKYLL_WORK_DIR=/srv/jekyll

# CONTAINER VOLUME - DB
MONGO_CFG_DIR=/etc/mongod
MONGO_DATA_DIR=/var/lib/mongo
MYSQL_CFG_DIR=/etc/mysql
MYSQL_DATA_DIR=/var/lib/mysql
MYSQL_RUNTIME_DIR=/run/mysqld
POSTGRES_CFG_DIR=/etc/postgresql-common
POSTGRES_DATA_DIR=/var/lib/postgresql/data
POSTGRES_LOG_DIR=/var/log/postgresql
ELASTIC_CFG_DIR=/etc/

# CONTAINER VOLUME - DATA PIPELINE
HADOOP_HOME=/hadoop_home/hadoop-3.2.1
SPARK_HOME=//hadoop_home/spark-3.0.0
HDFS_DATANODE_HOME=/hdfs/datanode
HDFS_NAMENODE_HOME=/hdfs/namenode

# PORT-CUSTOM
PORT_FRONTEND=44380
PORT_BACKEND=28282

# PORT - GERENAL
PORT_HTTP=80
PORT_HTTPS=443
PORT_SSH=22

# PORT - SERVICE
PORT_REACT=3000
PORT_DJANGO=8000
PORT_JUPYTER=8888
PORT_JEKYLL=4000
PORT_JEKYLL_INTERNAL=35729
PORT_MYSQL=3306
PORT_POSTGRES=5432
PORT_MONGO=27017
PORT_REDIS=6379
PORT_ELASTIC=60000
PORT_COUCH=5984
PORT_KAFKA=9092
PORT_HDFS_NAME=9000
PORT_HDFS_=7077
PORT_HDFS_=8021
PORT_HDFS_NAMENODE_WEB_UI=9870
PORT_YARN_RESOURCE_MANAGER_WEB_UI=8088
PORT_HDFS_HISTORY_SERVER_WEB_UI=19888



